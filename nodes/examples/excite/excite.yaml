name: excite
description: "An example interface for an echo program that takes input strings and returns excited versions of them"

inputs: 
  excite:
    str:
      type: string
      
outputs:
  excited:
    str:
      type: string
