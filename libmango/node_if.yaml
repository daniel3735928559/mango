get_if:
  args:
    type: dict
    values:
      if:
        type: string
  rets:
    type: dict
    values:
      doc:
        type: string
reg:
  args:
    type: dict
    values:
      node_id:
        type: string
      key:
        type: string

reply:
  args:
    type: any
